// Application schema entrypoint.
// Auth schema (Better Auth, generated) and app tables (projects, render_jobs)
// land here in the auth + render-pipeline phases.
//
// For now this file exports nothing. drizzle-kit will see an empty schema
// and produce no migrations until tables are added.
export {};
