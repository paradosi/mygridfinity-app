{
  "name": "@mygridfinity/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not yet configured (next pass)' && exit 0",
    "test": "echo 'no tests yet' && exit 0"
  },
  "dependencies": {
    "@mygridfinity/shared": "workspace:*",
    "bullmq": "^5.34.10",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
